// write a program to use a ternary operator to check if a number is even or odd and log the result to the console.


let number = 10;

let result = (number % 2 === 0 ) ? "Even Number" : "Odd Number";
console.log(result);