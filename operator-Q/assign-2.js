// use the -= operator to subtract the number from a variable and log the result to the console.

let subtract = 5;
console.log(subtract -= 3);

function check(a){
    let subtraction = a -= 3;
    console.log(subtraction);
}
check(5)